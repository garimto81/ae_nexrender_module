# ============================================================================
# AE-Nexrender Worker 환경변수 설정 예시
# ============================================================================
# 사용법:
# 1. 이 파일을 .env로 복사
# 2. 실제 값으로 변경
# 3. docker-compose up 실행
# ============================================================================

# ----------------------------------------------------------------------------
# Supabase 연동
# ----------------------------------------------------------------------------
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_KEY=eyJxxx

# ----------------------------------------------------------------------------
# Nexrender 연동
# ----------------------------------------------------------------------------
# Docker 내부에서 호스트의 Nexrender 서버 접근
NEXRENDER_URL=http://host.docker.internal:3000

# Nexrender API Secret (설정한 경우만)
NEXRENDER_SECRET=

# ----------------------------------------------------------------------------
# 경로 설정
# ----------------------------------------------------------------------------
# 출력 디렉토리 (Docker 호스트 경로)
OUTPUT_DIR_HOST=D:/output

# 템플릿 디렉토리 (Docker 호스트 경로)
TEMPLATE_DIR_HOST=D:/templates

# NAS 출력 경로 (Windows UNC 경로)
NAS_OUTPUT_PATH=//NAS/renders

# ----------------------------------------------------------------------------
# 렌더링 설정
# ----------------------------------------------------------------------------
# 렌더링 타임아웃 (초) - 기본 1800 (30분)
RENDER_TIMEOUT=1800

# 최대 재시도 횟수 - 기본 3
MAX_RETRIES=3

# ----------------------------------------------------------------------------
# 폴링 설정 (선택)
# ----------------------------------------------------------------------------
# 기본 폴링 주기 (초) - 기본 10
POLL_INTERVAL_DEFAULT=10

# 작업 처리 중 폴링 주기 (초) - 기본 5
POLL_INTERVAL_BUSY=5

# 작업 없을 때 폴링 주기 (초) - 기본 30
POLL_INTERVAL_IDLE=30

# ----------------------------------------------------------------------------
# 헬스체크 (선택)
# ----------------------------------------------------------------------------
# 헬스체크 서버 포트 - 기본 8080
HEALTH_PORT=8080

# ----------------------------------------------------------------------------
# 개발/디버깅 (선택)
# ----------------------------------------------------------------------------
# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# 워커 ID 고정 (디버깅용)
# WORKER_ID=fixed-worker-id

# ----------------------------------------------------------------------------
# 테스트용 (CyprusDesign 템플릿)
# ----------------------------------------------------------------------------
# 테스트용 AEP 파일 경로 (Docker 호스트 경로)
TEST_AEP_PATH=D:/templates/CyprusDesign/CyprusDesign.aep

# 테스트용 컴포지션 이름
TEST_COMPOSITION=1-Hand-for-hand play is currently in progress

# ----------------------------------------------------------------------------
# 경로 매핑 (고급)
# ----------------------------------------------------------------------------
# 커스텀 경로 매핑 (형식: docker_path:windows_path,...)
# PATH_MAPPINGS=/app/templates:C:/templates,/app/output:D:/output
